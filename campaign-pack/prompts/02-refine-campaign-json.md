Edit `campaign.json` only.

Preserve the JSON schema exactly. Do not create HTML, CSS, JavaScript, SVG, or renderer files.

Apply the requested design direction by changing only structured campaign values:
- copy tone and length
- theme colors
- layout mood
- asset references
- notes for critique or art direction

Keep these constraints:
- headline under 9 words
- subhead under 26 words
- local relative asset paths only
- valid JSON only
- no markdown fences inside the file

After editing, verify `campaign.json` exists and is valid JSON, then reply DONE.
